# SSL/TLS 최종 멘탈 모델 확립

사용자는 SSL/TLS 학습 흐름을 마무리하며 HTTPS를 “HTTP over TLS”, 인증서를 “도메인과 공개키에 대한 CA 서명 보증서”, session key를 “TLS 연결 동안만 쓰는 임시 대칭키 계열”로 구분할 수 있게 되었다. 이후 웹 보안 세션에서는 이 Topic Summary를 기준 문서로 삼고, 쿠키 Secure/HttpOnly/SameSite나 OAuth2 redirect 보안을 TLS 전송 보호 위에 연결하면 된다.
