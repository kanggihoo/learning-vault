# HTTPS, TLS, 인증서 질문 기준점

사용자는 HTTPS가 HTTP보다 권장된다는 사실은 알고 있으며, 이번 Teach Session에서는 “HTTP가 HTTPS로 변환되는가”, “무엇이 암호화되고 무엇이 노출되는가”, “SSL 인증서가 암복호화 과정에서 어떤 역할을 하는가”를 구분하는 단계에 있다. 앞으로 웹 인증과 쿠키 보안을 다룰 때는 TLS가 HTTP 메시지를 감싸는 전송 계층 보호라는 전제를 다시 연결하면 좋다.
