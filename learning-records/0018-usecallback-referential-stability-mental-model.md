# useCallback 참조 안정성 멘탈 모델

사용자는 `useCallback`을 함수 호출 결과 캐시가 아니라 자식에게 넘기는 함수 prop의 참조를 안정화해 `memo`된 자식의 불필요한 렌더링을 줄이는 도구로 이해하기 시작했다. 앞으로는 “부모가 렌더링되면 일반 자식은 다시 렌더링되지만, `memo`된 자식은 props 얕은 비교로 건너뛸 수 있고 이때 함수 prop 참조 안정성이 중요하다”는 기준으로 설명한다.
