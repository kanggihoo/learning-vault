# React Hooks 선택 기준 질문 시작

사용자는 `useEffect`, `useRef`, `useMemo`, `useCallback`, `useReducer`를 각각 한 줄로는 알고 있지만 실제로 언제 어떻게 골라야 하는지 자주 헷갈린다고 밝혔다. 앞으로 React Hook 설명은 “Hook 이름”보다 “외부 동기화, 렌더링 없는 보관, 값 캐시, 함수 캐시, 복잡한 상태 규칙”이라는 선택 기준으로 연결해 설명한다.
