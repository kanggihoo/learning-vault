# Notes

- React Hook 학습은 단순 정의보다 실제 상황과 실제 코드 예제를 함께 보여줄 때 더 직관적이다.
- 분산 백엔드 아키텍처 학습은 미션이 "이해·설명"이다. 구현 코드보다 Sixmen 아키텍처 문서를 구체 예시로 쓰고, 개념 4개(Outbox → Streams Consumer Group → At-least-once+재시도 → Webhook)를 의존성 순으로 한 레슨에 하나씩 푼다.
- 새 주제를 시작할 때 이미 아는 기술(Docker 등)과의 대응표를 먼저 깔면 이해가 빠르다. 쿠버네티스 첫 레슨(0015)도 Docker 앵커 방식으로 구성했다.
- 구조를 배우는 요청에는 SVG 다이어그램을 직접 그려 넣는다. 텍스트 설명만으로는 "전체 지도"가 안 잡힌다.
- 사용자의 질문 표현 자체에 개념적 오해가 숨어 있을 때가 있다. 모의투자 WS 세션(0031)에서 "사용자마다 다른 정보"가 그랬다 — 레슨 §0을 그 오해를 깨는 데 먼저 쓰고, 나머지를 그 위에 얹으니 구조가 잡혔다.
- 설계가 크게 갈리는 전제(예: 재생 시계가 공용인가 개인인가)는 레슨을 만들기 전에 AskUserQuestion으로 먼저 확정한다. 나중에 고치면 레슨 전체를 다시 써야 한다.
- 재사용 컴포넌트로 `assets/quiz.js`(퀴즈 채점)와 `assets/diagram.css`(SVG 다이어그램 공통 스타일: `.d-box`, `.d-line`, `.d-label` 등)를 만들었다. 새 레슨은 인라인 스크립트/스타일 대신 이 둘을 링크한다.
- "코드로 설명하지 말라"는 요청은 **구현 예제 금지**이지 **타입 시그니처 금지**가 아니다. 0032에서 `Message`/`MessageChannel`/`MessageHandler`의 인터페이스 정의 3줄을 보여준 것이 오히려 이해를 가장 크게 앞당겼다.
- 사용자가 되묻는 질문 안에 맞는 직관과 틀린 전제가 섞여 있을 때, **맞는 것부터 "맞다"고 확인해주고 하나만 보정**하는 방식이 잘 통했다 (0032: Handler=콜백 맞음 → 단 상태를 가짐 / HTTP 업그레이드 맞음 → 단 같은 TCP 재사용).
- 사용자가 외부에서 가져온 설명을 레슨에 넣어달라고 할 때는 **그대로 붙이지 말고 검증한다.** 0032 §6(모듈 경계)에서 가져온 자료의 90%는 맞았지만 "STOMP 파싱은 spring-websocket이 한다"는 부분이 틀렸다(실제로는 `StompDecoder`도 spring-messaging). 보정하니 오히려 결론이 강해졌다.
- **개념 → 어휘 → 코드 매핑** 순서가 잘 통한다. 0031(흐름) → 0032(어휘) → 0033(코드)로 갔더니, 같은 `enableSimpleBroker()` 한 줄이 "브로커 빈을 만들어 두 채널에 등록한다"로 읽혔다. 코드를 먼저 보여줬으면 그냥 외웠을 것.
- 비유를 여러 레슨에 걸쳐 쓰면 **비유↔실제 클래스 매핑표로 정산하는 절**이 반드시 필요하다 (0033 §3). 사용자가 "그 장부가 SimpleBroker냐"고 직접 물어온 게 신호였다.
- 사용자가 가져오는 외부 자료는 **큰 그림은 대체로 맞고 세부 하나가 틀린** 패턴이 반복된다(0032 §6의 STOMP 파싱 위치, 0034의 `/queue` 의미). 그 틀린 하나가 보통 "그래서 무엇이 어디에 속하는가"라 검증 가치가 높다.
- 처음에 out of scope로 잡은 항목도 사용자가 직접 물으면 편입하고 MISSION을 고친다(0034: 외부 브로커 릴레이). 대신 편입 범위를 좁게 다시 적는다 — "비교·판단까지, 실제 구축·운영은 제외".
- 레슨 아크가 길어지면 마지막 레슨에 **"증상 → 어느 레슨 어느 절" 진단 표**를 넣는 게 좋다(0035 §6). 흩어진 지식을 하나의 실전 체크리스트로 회수해준다.
- 앞 레슨에서 가르친 사실이 뒤 레슨에서 **설계 제약으로 되돌아오는** 지점을 하이라이트로 삼으면 기억에 오래 남는다(0031 §7의 "UNSUBSCRIBE엔 destination이 없다" → 0035의 구독자 카운터 구현 함정).
- 문서가 이미 "결정 근거"를 산문으로 적어 둔 프로젝트에서는 **문서 요약이 레슨이 될 수 없다.** 마켓 리플레이(0036)가 그랬다 — `_common.md`·`ord.md`·`acc.md`가 왜 그렇게 했는지를 전부 설명해 둔다. 가르칠 것은 그 밑의 전이 가능한 개념, 그리고 **문서가 틀리거나 부정확한 지점**이다.
- **"AI가 코드를 쓰니 개념만 이해하고 싶다"는 요청의 표적은 리뷰 능력이다.** 그래서 레슨은 "AI가 이걸 써 왔을 때 어떻게 지적하나"까지 가야 한다. 0036 §3이 그 형태다 — `fixedRate`를 "드리프트 때문에 안 된다"고 지적하면 AI가 반박할 수 있고 그 반박이 맞다. **틀린 이유로 옳은 결론을 내면 다음 리뷰에서 설득력을 잃는다.**
- **검색 스니펫으로 "문서가 틀렸다"를 쓰지 않는다. 원문을 fetch한다.** 0036에서 WebSearch 스니펫이 "WebSocket 쓰는 탭은 스로틀 예외"라고 해서 기획서 논거를 반박하는 절을 쓰려 했는데, MDN 원문에는 그 예외가 없었다(WebRTC·AudioContext만). 스니펫을 믿었으면 레슨에 틀린 절이 들어갔다.
- 문서가 맞다고 확인되면 **더 정밀하게 만들어 주는 것**이 correction보다 가치 있다. 기획서의 "초당 1회 → 분당 1회"는 정확했고, MDN의 실제 조건(비가시 5분↑ + 무음 30초↑ + 체인 5단↑ + WebRTC 비활성)과 Firefox Android 15분·탭 언로드를 표로 얹었다.
- 설계 결정이 **관측 가능성을 만드는 지점**은 좋은 레슨 재료다. 절대시각 스케줄링이라 "예정 − 실제"가 그대로 틱 지연 지표가 되고, 간격 기반이면 밀린 것이 새 기준이 되어 지연이 안 잡힌다(0036 §5).
- 서버와 클라이언트가 **같은 원칙을 다른 층에서** 쓰는 구조를 찾으면 레슨을 한 문장으로 회수할 수 있다(0036: `tickAt(k)`와 클라이언트 오프셋이 둘 다 "이전 값에 더하지 말고 절대 기준에서 다시 구한다").
- **방대한 실제 프로젝트를 가르칠 때는 개념 레슨보다 "지도" 레퍼런스가 먼저다.** 마켓 리플레이에서 이걸 틀렸다 — 11레슨 아크를 전이 가능한 개념만으로 짜고 전체 조망 산출물을 안 만들었더니, 사용자가 "내용이 되게 많지 않았나? 이것만 이해하면 되냐"고 두 번 되물었다. 정당한 지적이었다. 지도가 없으면 첫 레슨이 어디에 속하는지 알 수 없어 **공중에 뜬다.**
- **두 종류의 이해를 구분해서 산출물을 나눈다.** Type A(원리: 전이됨, 양 적음, AI가 자주 틀림) → **레슨.** Type B(이 프로젝트의 사실: 전이 안 됨, 방대함, AI는 문서 보고 정확히 씀) → **레퍼런스/지도.** Type B를 레슨으로 만들면 암기 과제가 되어 실패하고, Type A만 주면 조망이 없어 실패한다.
- **"이 디렉토리의 .md를 읽어라"를 문자 그대로 수행하면 계약의 반쪽을 놓칠 수 있다.** 마켓 리플레이에서 `schema.sql`(같은 디렉토리인데 확장자가 달라 빠짐)과 `backend/docs/conventions/code.md`(응답 봉투·예외를 위임받은 정본)를 놓쳤다. **읽기 단계에서 인용 링크를 따라가 "이 문서가 어디로 위임하는지"를 먼저 확인한다.**
- 사용자가 되물으면 **첫 해석을 의심한다.** "이것만 읽으면 되는거야?"를 나는 "레슨 하나로 충분한가"로 읽었지만 실제 뜻은 "문서는 방대한데 왜 이것뿐이냐"였다. 같은 질문을 두 번 하는 것 자체가 첫 답이 표적을 벗어났다는 신호다.
- **가르친 개념이 실제 도구가 된 순간은 반드시 기록하고 사용자에게 명시한다.** 0036의 720초 그리드 검산을 IPO 편성표에 적용해 `종목.md`와 기능명세서의 시각 불일치를 30초에 찾았다(8건 전부, 12분 배수 위반). 이런 사례가 "왜 개념을 배우는가"에 대한 유일하게 설득력 있는 답이다.
- 아키텍처 미션(0037~)은 **지도(레퍼런스 0020)를 레슨보다 먼저** 만들고 시작했다. 0030의 교훈을 처음부터 적용한 첫 사례다. 레슨 헤더에 "지도의 §N을 판다"를 적어 항상 위치를 알 수 있게 했다.
- 자가 채점·모범답안은 **JS 대신 native `<details>`**로 만들었다(`assets/lesson.css`의 `details.reveal`). 새 컴포넌트를 만들 이유가 없었고, 인쇄 시에도 열린다.
- 아키텍처 용어는 **문헌마다 경계가 흔들린다**(CQRS는 MS 카탈로그에선 패턴, Richards 책에선 스타일). 이름 논쟁 대신 **판별 기준 하나**("이 결정이 시스템 전체의 요소·관계 규칙을 바꾸는가")를 주는 쪽이 훨씬 잘 통한다.
- **"AI 설계안 리뷰"라는 목표는 레슨의 결론 형태를 바꾼다.** 개념 설명으로 끝내지 않고 "AI가 이렇게 답할 때 무엇을 물으면 무너지는가"까지 가야 한다. 0037 §5의 "빈칸을 가리키는 일이라 반박당하지 않는다"가 그 형태.
- 패턴 레슨(0040)은 **카탈로그 투어를 거부하는 것**이 설계 결정이었다. 40개 목록은 찾아보면 있고 외워도 못 쓴다. 대신 ① 증상이 아니라 **원인**이 패턴을 고른다(같은 "느리다"에 5개 패턴) ② **패턴은 혼자 오지 않는다**(조합 체인) 두 개로 좁혔다.
- 조합 다이어그램에서 **Idempotent Consumer가 세 체인 모두에 나오는 것**이 우연이 아니라는 지적이 잘 통하는 회수 지점이다 — 이미 배운 Outbox·Redis Streams가 그 자리에 있었다는 걸 뒤늦게 이름 붙여 준다.
- Saga의 **피벗 트랜잭션**(보상 가능 / 피벗 / 재시도 가능 3분류)은 실무 가치가 큰데 거의 안 가르쳐진다. "결제 승인이 피벗이죠? 그럼 그 뒤는 보상이 아니라 재시도여야 합니다"가 바로 쓰는 리뷰 문장이 된다.
- **"전부 포함해줘"에 한 레슨으로 답하지 않았다.** 미결 8개를 주제별로 묶어 4레슨(경계 / 멱등성 / 숫자 / 검증·기록)으로 나눴다. 한 레슨에 다 넣으면 working memory를 넘겨 미션 제약 자체를 어긴다. **범위를 늘리라는 요청은 레슨을 키우라는 뜻이 아니라 레슨을 더 만들라는 뜻이다.**
- **레슨 말미의 "막히면 물어보자" 목록이 다음 커리큘럼이 된다.** 매 레슨에서 파고들 만한 곳 3~4개를 적어 뒀더니, 사용자가 "전부"라고 했을 때 아크가 이미 설계되어 있었다. 이 습관은 계속 유지한다.
- 아크가 길어지면 **마지막 레슨의 진단 표**(증상 → 어느 레슨 어느 절)와 **별도 질문 카드 레퍼런스**를 둘 다 만든다. 진단 표는 학습 회수용, 질문 카드는 실전 중에 여는 용도라 쓰임이 다르다.
- **사용자가 "진행"만 반복할 때는 이해 확인이 0이다.** 8레슨 내내 그랬다. 아크가 끝난 지금 다음 할 일은 새 주제가 아니라 **실제 문서를 이 어휘로 같이 읽어 보는 것**이다. 안 하면 "다뤘지만 못 쓰는" 지식으로 끝난다.
- **"진행"이 반복될 때는 사용자가 검증할 수 있는 구체적 주장을 만들어 주는 쪽으로 간다.** 0045에서 실제 프로젝트 문서를 리뷰해 R1~R4라는 반박 가능한 주장 4개를 만들었다. 추상 레슨을 하나 더 얹는 것보다 이게 대화를 되살릴 확률이 높다.
- **리뷰 산출물은 비위험(nonrisk)을 먼저 쓴다.** 0044 §2의 네 산출물 중 비위험을 빼면 어떤 리뷰도 공격으로 읽힌다. 그리고 좋은 결정에 이름이 없으면 나중에 누가 바꾸려 할 때 지킬 수 없다 — 이건 0044를 실제로 써 보고 알았다.
- **가장 값싼 고품질 리뷰 기법: 같은 팀이 한 곳에서 내린 좋은 판단을 다른 곳에도 적용했는지 대조한다.** 0045의 R3(주문 멱등성)가 `ipo.md`의 자기 문장에서 나왔다. 문서를 처음부터 판단하는 것보다 훨씬 싸고, 반박당하지 않는다("당신들이 옳게 쓴 문장입니다").
- 사용자의 프로젝트 문서는 `/Users/kkh/Desktop/S15P21A605`에 있다 (`backend/docs/` = ADR·CONTEXT·overview·decisions-later, `docs/projects/market-replay-api/` = API 계약 20편). **문서 품질이 매우 높다** — ADR에 "감수하는 것"이 실제로 채워져 있다. 리뷰할 때 이 전제에서 시작한다.
